import type { Task, Habit, WaterLog, SleepLog } from './types'

export const initTasks: Task[] = []

export const initHabits: Habit[] = []

export function initWaterLog(): WaterLog {
  return {}
}

export function initSleepLog(): SleepLog {
  return {}
}
